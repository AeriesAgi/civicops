{
  "name": "civicops-band-bridge",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Node sidecar that publishes CivicOps Command agent messages to Band via @band-sdk/core. The C# agents relay every Band message here, and this service speaks to the real Band platform with the official SDK.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "@band-sdk/core": "^1.0.0",
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18"
  }
}

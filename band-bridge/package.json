{
  "name": "civicops-band-bridge",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "description": "Node sidecar that publishes CivicOps Command's five-agent dispatch transcript into a real Band room using the official Band SDK (@band-ai/sdk + @thenvoi/rest-client). The C# agents relay every Band message here; this service speaks to the live Band platform.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "@band-ai/sdk": "^0.1.6",
    "@thenvoi/rest-client": "^0.0.113",
    "express": "^5.2.1"
  },
  "engines": {
    "node": ">=18"
  }
}
